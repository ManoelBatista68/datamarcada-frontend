function salvarArquivoDrive(base64Data, fileName, mimeType) { try { const data = base64Data.split(',')[1]; const blob = Utilities.newBlob(Utilities.base64Decode(data), mimeType, fileName); let folder; const folderIterator = DriveApp.getFoldersByName("AppAgenda_Anexos"); if (folderIterator.hasNext()) { folder = folderIterator.next(); } else { folder = DriveApp.createFolder("AppAgenda_Anexos"); } const file = folder.createFile(blob); file.setSharing(DriveApp.Access.ANYONE_WITH_LINK, DriveApp.Permission.VIEW); return { success: true, url: file.getUrl() }; } catch(e) { return { success: false, error: e.toString() }; } }
function forcarPermissaoDrive() { var pastaTemp = DriveApp.createFolder("Ativador_De_Permissao"); pastaTemp.setTrashed(true); }
